import { LatestChartComponent } from './latest-chart.component';

describe('LatestChartComponent', () => {
  it('should create the component (simple test)', () => {
    const component = new LatestChartComponent({} as any); 
    expect(component).toBeTruthy(); 
  });
});
