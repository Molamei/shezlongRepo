import { DashboardComponent } from './dashboard.component';

describe('DashboardComponent', () => {
  it('should create the component (simple test)', () => {
    const component = new DashboardComponent();
    expect(component).toBeTruthy(); 
  });
});
